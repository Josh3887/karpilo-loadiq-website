import { LaunchMarketingPage } from "@/components/marketing/marketing-pages";

export const metadata = {
  title: "Launch | Karpilo LoadIQ",
  description:
    "Controlled Karpilo LoadIQ launch access, rollout phases, request-access status, demo routing, and app portal handoff.",
};

export default function Page() {
  return <LaunchMarketingPage />;
}
